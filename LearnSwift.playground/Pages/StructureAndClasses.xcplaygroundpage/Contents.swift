// struct are value type
// and classes and refernece type
// Classes can inherit each other
// structure will have intializer created to set value for each property
//

// Identity Operator :: used with classes
// === equal to
// !== not equal to
// identity operators are used to check if two different var are refereing to same class object i.e thier memory location is same
// since == and != check


// Equalance operator ::
// ==  and != need to confirm to Equatable protocol and override
// == OR != operator 
